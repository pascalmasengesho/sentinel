# Contributing

Thanks for improving Sentinel. Keep all changes compatible with authorized, non-destructive assessment use. Do not contribute exploitation, authentication bypass, credential testing, evasion, denial-of-service, or state-changing features.

Run `make format` and `make check` before opening a pull request. Include unit tests without live external scans, describe the target traffic a new feature produces, and update the documentation for user-visible behavior.

